@model List<Programme>
@{
    ViewData["Title"] = "Home Page";
}


<h1>Welcome to the StarSat TV Guide</h1>
<p>Consult your TV program of this day and find out which TV shows are available on StarSat satellite receivers in Tunisia and North Africa that use sharing servers such as Forever, GShare, Vanilla and FunCam.</p>



<h2>Now on TV</h2>
<table class="table table-hover">
    <tbody>
        @foreach(Programme programme in Model)
        {
                        DateTime testdate = new DateTime(2022, 04, 16, 17, 30, 00);
         int programLength = (@programme.Stop - @programme.Start).Minutes;            int programProgress = (testdate - @programme.Start).Minutes;
            double percentage = 0;
            if(programLength > 0)   
                percentage = programProgress * 100 / programLength;

            <tr>
            <td><img src="@programme.Channel.Logo" width="35"/> @programme.Channel.Name</td>
            <td>@programme.Start.ToString("HH:mm") - @programme.Stop.ToString("HH:mm") - <b>@programme.Title</b></td>
            <td scope="row">
                                <div class="progress" style="width: 500px; height: 5px">
                  <div class="progress-bar bg-danger" role="progressbar" style="width: @percentage%;"></div>
                </div>
            </td>
        </tr>
        }
    </tbody>
</table>

